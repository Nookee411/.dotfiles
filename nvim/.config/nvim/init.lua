-- bootstrap lazy.nvim, LazyVim and your plugins
require("config.lazy")

-- require("nvim-tree").setup({
--   filters = {
--     dotfiles = true,
--     git = {
--       enable = true,
--       ignore = false,
--       timeout = 500,
--     },
--   },
-- })
